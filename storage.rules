function isLessThanNMegabytes(n) {
	return request.resource.size < n * 1024 * 1024; 
}

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && isLessThanNMegabytes(0.5);
    }
  }
}